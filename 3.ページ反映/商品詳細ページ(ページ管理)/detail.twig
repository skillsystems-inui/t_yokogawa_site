{#
This file is part of EC-CUBE

Copyright(c) EC-CUBE CO.,LTD. All Rights Reserved.

http://www.ec-cube.co.jp/

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.
#}
{% extends 'default_frame.twig' %}

{% set body_class = 'product_page' %}

{% block stylesheet %}
    <style>

        .slick-slider {
            margin-bottom: 30px;
        }

        .slick-dots {
            position: absolute;
            bottom: -45px;
            display: block;
            width: 100%;
            padding: 0;
            list-style: none;
            text-align: center;
        }

        .slick-dots li {
            position: relative;
            display: inline-block;
            width: 20px;
            height: 20px;
            margin: 0 5px;
            padding: 0;

            cursor: pointer;
        }

        .slick-dots li button {
            font-size: 0;
            line-height: 0;
            display: block;
            width: 20px;
            height: 20px;
            padding: 5px;
            cursor: pointer;
            color: transparent;
            border: 0;
            outline: none;
            background: transparent;
        }

        .slick-dots li button:hover,
        .slick-dots li button:focus {
            outline: none;
        }

        .slick-dots li button:hover:before,
        .slick-dots li button:focus:before {
            opacity: 1;
        }

        .slick-dots li button:before {
            content: " ";
            line-height: 20px;
            position: absolute;
            top: 0;
            left: 0;
            width: 12px;
            height: 12px;
            text-align: center;
            opacity: .25;
            background-color: black;
            border-radius: 50%;

        }

        .slick-dots li.slick-active button:before {
            opacity: .75;
            background-color: black;
        }

        .slick-dots li button.thumbnail img {
            width: 0;
            height: 0;
        }

    </style>
{% endblock %}

{% block javascript %}
    <script>
	    window.onload = function(){
			//オプション入力欄をセットする
			$form = $('#form1');
			eccube.setOptionText($form);
		}
		
		/**
	     * オプション入力欄をセットする.
	     */
	     eccube.getPerOptionText = function($no, $form, $optn, $opt_id) {
	        
	        /*
	        * 写真付きオプション用のパス一覧
	        *  ※画像はファイル管理で登録されているものを指定してください
	        */
	        var evm = location.href.indexOf('free') != -1 ? '/eccube' : '';
	        
	        //キャンドル
	        var optimg_candle_big     = '/html/user_data/assets/img/option/opt_candle_big.png';//キャンドル大
	        var optimg_candle_small   = '/html/user_data/assets/img/option/opt_candle_small.png';//キャンドル小
	        var optimg_candle_no      = '/html/user_data/assets/img/option/opt_candle_no.png';//キャンドルNo
	        //メッセージ
	        var optimg_message_muji          = '/html/user_data/assets/img/option/opt_plate_muji.png';//無地プレート
	        var optimg_message_omedetou_only = '/html/user_data/assets/img/option/opt_plate_omedeto_only.png';//おたんじょうびおめでとう
	        var optimg_message_omedetou_plus = '/html/user_data/assets/img/option/opt_plate_omedeto_plus.png';//おたんじょうびおめでとう(□に名前が入ります)
	        //デコレーション追加
	        var optimg_decoadd_ichigo     = '/html/user_data/assets/img/option/opt_adddeco_ichigo.png';//いちごUP
	        var optimg_decoadd_fruit      = '/html/user_data/assets/img/option/opt_adddeco_fruit.png';//フルーツUP
	        var optimg_decoadd_choko_nama = '/html/user_data/assets/img/option/opt_adddeco_choko_nama.png';//生チョコUP
	        var optimg_decoadd_choko_e    = '/html/user_data/assets/img/option/opt_adddeco_choko_e.png';//絵チョコ (4号)
	        var hskimg_decoadd_choko_e    = '/html/user_data/assets/img/option/setumei_choko_nama.png';//絵チョコ (4号) (補足)
	        //包装方法
	        var optimg_housohouho_housou    = '/html/user_data/assets/img/option/opt_gift_housou.png';//包装
	        var optimg_housohouho_jitaku    = '/html/user_data/assets/img/option/opt_gift_jitaku.png';//自宅用
	        var optimg_housohouho_ribon     = '/html/user_data/assets/img/option/opt_gift_ribon.png'; //リボン
	        var optimg_housohouho_noshi     = '/html/user_data/assets/img/option/opt_gift_noshi.png'; //熨斗
	        //--------------------------------------
	        
	        //オプション選択
	        var $optn_select_text = $optn.text();
	        var lbl_id = $opt_id + '_lbl';
	        var lbl_info = document.getElementById(lbl_id);
	        if(lbl_info != null){
	        	//〇キャンドルの場合
	        	if (lbl_info.textContent.indexOf('ろうそく') != -1 || lbl_info.textContent.indexOf('キャンドル') != -1) {
	        		var innner_id = $opt_id + '_nyuryoku';
	        		
	        		var innnerElm = document.getElementById(innner_id);
	        		if(innnerElm != null){
		        		
		        		//1.入力項目制御(数字選択)
		        		
		        		//数字選択欄を表示させる
		        		innnerElm.style.display="block";
		        		//ろうそくならラジオボタン入力欄を非表示
		        		var orgblock_id = $opt_id + '_wdg';
		        		if(document.getElementById(orgblock_id) != null){
	        				//document.getElementById(orgblock_id).style.display="block";
	        				document.getElementById(orgblock_id).style.display="none";
	        			}
	        			
	        			//2.大カテゴリタイトルセット
	        			//ToDo ろうそく関連の先頭の判断をすればいいのでは？
	        			//ろうそく大をろうそく関連のメインラベルで出す
	        			if (lbl_info.textContent.indexOf('大') != -1) 
	        			{
	        				var inlbl_id = $opt_id + '_mainlabel';
			        		var innnerLbl = document.getElementById(inlbl_id);
			        		if(innnerLbl != null){
			        				//タグ作成
					        		let lbltag ='<div style="background-color:lightblue; font-size:21px;">';
					        		lbltag = lbltag + '<div class="" style="float:left; width:80%; padding:8px;"><b>キャンドルを選択</b></div>';
			        				lbltag = lbltag + '<div style="float:left; width:20%; padding:8px; font-size:16px;"><input type="checkbox" id="chk_candle_nosel" name="chk_candle_nosel"><label for="chk_candle_nosel">不要</label></div>';
			        				lbltag = lbltag + '<div style="clear:both;"></div>';
			        				lbltag = lbltag + '</div>';
			        				
			        				//タグを埋め込む
					        		//innnerLbl.innerHTML = lbltag;
					        		innnerLbl.insertAdjacentHTML('afterend', lbltag);
			        		}
	        			}
	        			
			        	//3.オプション画像セット
			        	{
			        		//ラジオボタン選択の項目なので先頭に画像をセットさせる
			        		// 対象のID名(xxx_gazou)
			        		var item_id_target = $opt_id + '_gazou';
			        		// あるかチェック
			        		var target_info = document.getElementById(item_id_target);
							if(target_info != null){
								// div要素を作成
								var img_element = document.createElement('div');
								//-- 画像の判定 --
								//　　対象の画像パス
								var target_img_pass = '';
								
								//オプション名から判定する
								if(lbl_info.textContent.indexOf('大') != -1){
									//キャンドル大
									target_img_pass = evm + optimg_candle_big;
									//サイズ
									img_element.style.width  = '100px';
									img_element.style.height = '100px';
								}else if(lbl_info.textContent.indexOf('小') != -1){
									//キャンドル小
									target_img_pass = evm + optimg_candle_small;
									//サイズ
									img_element.style.width  = '100px';
									img_element.style.height = '100px';
								}else if(lbl_info.textContent.indexOf('N') != -1){
									//Noキャンドル
									target_img_pass = evm + optimg_candle_no;
									
									//2つ目以降は出さない
									if(lbl_info.textContent.indexOf('1') != -1){
										//サイズあり
										img_element.style.width  = '280px';
										img_element.style.height = '155px';
									}else{
										//サイズなし
										img_element.style.width  = '0px';
										img_element.style.height = '0pxx';
									}
								}
								// 画像パスセット
								 
								img_element.style.backgroundImage = 'url(' + target_img_pass + ')'; 
								//指定要素(gazou)の前にセット
								target_info.before(img_element);
							}
						}
						
						//4.項目名セット
	        			{
	        				var kmkm_id = $opt_id + '_koumokumei';
			        		var innnerKmk = document.getElementById(kmkm_id);
			        		if(innnerKmk != null){
			        				//タグ作成
					        		let lbltag ='<div style="font-size:21px;">';
					        		
					        		if (lbl_info.textContent.indexOf('N') != -1 && lbl_info.textContent.indexOf('1') != -1) {
						        		//Noキャンドルの場合
						        		lbltag = lbltag + '<div style="float:left; padding:8px;">Noキャンドル</div>';
						        		lbltag = lbltag + '<div style="float:left; padding:8px; color:red;">(+120円)</div>';
				        				lbltag = lbltag + '<div style="float:left; padding:8px; background-color:red; color:#fff;">有料</div>';
			        				}else if(lbl_info.textContent.indexOf('大') != -1) {
			        					//キャンドル大の場合
						        		lbltag = lbltag + '<div style="float:left; padding:8px;">キャンドル　大</div>';
			        				}else if(lbl_info.textContent.indexOf('大') != -1) {
			        					//キャンドル小の場合
						        		lbltag = lbltag + '<div style="float:left; padding:8px;">キャンドル　小</div>';
			        				}
			        				lbltag = lbltag + '<div style="clear:both;"></div>';
			        				lbltag = lbltag + '</div>';
			        				
			        				//タグを埋め込む
					        		innnerKmk.insertAdjacentHTML('afterend', lbltag);
			        		}
	        			}

	        		}
	        	}else if(lbl_info.textContent.indexOf('追加') != -1){
	        		
	        		//〇デコレーション追加入力の場合
	        		var innner_id = $opt_id + '_nyuryoku';
	        		
	        		//1.入力項目制御(数字選択)
	        		var innnerElm = document.getElementById(innner_id);
	        		if(innnerElm != null){
		        		
		        		//数字選択欄を表示させる
		        		innnerElm.style.display="block";
		        		
		        		//デコレーション追加ならラジオボタン入力欄を非表示
		        		var orgblock_id = $opt_id + '_wdg';
		        		if(document.getElementById(orgblock_id) != null){
	        				//document.getElementById(orgblock_id).style.display="block";
	        				document.getElementById(orgblock_id).style.display="none";
	        			}
	        		}
	        		//2.画像と大カテゴリタイトルセット
		        	{
		        		//ラジオボタン選択の項目なので先頭に画像をセットさせる
		        		// 対象のID名
		        		var item_id_target = $opt_id + '_lbl';
		        		// あるかチェック
		        		var target_info = document.getElementById(item_id_target);
				        // --項目の数だけ繰り返し--
				        if(target_info != null) {
							// div要素を作成
							var img_element = document.createElement('div');
							// 補足要素を作成
							var img_hosoku = document.createElement('div');
							img_hosoku.style.width  = '0px';
							img_hosoku.style.height = '0px'; 
							
							//ToDo テキスト入力項目のラベル名の取り方は？　いったん想定される項目の中から選ぶ
							
							//.ラベル取得
							
							//-- 画像の判定 --
							//　　対象の画像パス
							var target_img_pass = '';
							//    補足画像パス
							var hosoku_img_pass = '';
							//  ラジオボタン名から判定する
							if(lbl_info.textContent.indexOf('いちご') != -1){
								//いちごUP
								target_img_pass = evm + optimg_decoadd_ichigo;
							}else if(lbl_info.textContent.indexOf('フルーツ') != -1){
								//フルーツUP
								target_img_pass = evm + optimg_decoadd_fruit;
							}else if(lbl_info.textContent.indexOf('生チョコ') != -1){
								//生チョコUP
								target_img_pass = evm + optimg_decoadd_choko_nama;
							}else if(lbl_info.textContent.indexOf('絵チョコ') != -1){
								//絵チョコ(4号)
								target_img_pass = evm + optimg_decoadd_choko_e;
								img_hosoku.style.width  = '350px';
								img_hosoku.style.height = '200px'; 
								hosoku_img_pass = evm + hskimg_decoadd_choko_e;
							}
							// 画像パスセット
							img_element.style.width  = '160px';
							img_element.style.height = '160px'; 
							img_element.style.backgroundImage = 'url(' + target_img_pass + ')'; 
							
							//指定要素の前にセット
							target_info.before(img_element);
							//------------
							//補足用画像は後ろにつける
							img_hosoku.style.backgroundImage = 'url(' + hosoku_img_pass + ')'; 
							//指定要素の後ろにセット
							target_info.after(img_hosoku);
							
						}
						// --.項目の数だけ繰り返し--
					
						//オプション大タイトルを付ける(デコレーション追加)
						//ToDo 先頭の判断をすればいいのでは？
	        			//いちごをデコレーション追加関連のメインラベルで出す
	        			if (lbl_info.textContent.indexOf('いちご') != -1) 
	        			{
	        				var inlbl_id = $opt_id + '_mainlabel';
			        		var innnerLbl = document.getElementById(inlbl_id);
			        		if(innnerLbl != null){
			        				//タグ作成
					        		let lbltag ='<div style="background-color:lightblue; font-size:21px;">';
					        		lbltag = lbltag + '<div class="" style="float:left; width:80%; padding:8px;"><b>デコレーション追加</b></div>';
			        				lbltag = lbltag + '<div style="float:left; width:20%; padding:8px; font-size:16px;"><input type="checkbox" id="chk_message_nosel" name="chk_message_nosel"><label for="chk_message_nosel">不要</label></div>';
			        				lbltag = lbltag + '<div style="clear:both;"></div>';
			        				lbltag = lbltag + '</div>';
			        				
			        				//タグを埋め込む
					        		//innnerLbl.innerHTML = lbltag;
					        		innnerLbl.insertAdjacentHTML('afterend', lbltag);
			        		}
	        			}
					}
	        		//------------------------------------------------------------------------------------------------------------
	        	
	        	}else{
	        		//ろうそく意外なら数字入力欄は非活性にする
	        		var innum_id = $opt_id + '_num';
	        		var innumElm = document.getElementById(innum_id);
	        		if(innumElm != null){
	        			//非活性にする
	        			innumElm.disabled = true;
	        		}
	        		
	        		//〇メッセージ入力の場合
	        		if (lbl_info.textContent.indexOf('プレート') != -1) {
		        		//ラジオボタン選択の項目なので先頭に画像をセットさせる
		        		// ラジオボタンの項目はidとして「」のように持っている
		        		var item_id_base = $opt_id + '_';
		        		var item_cnt = 0;//初期値は0番目
		        		// 対象のID名
		        		var item_id_target = $opt_id + '_' + item_cnt;
		        		// あるかチェック
		        		var target_info = document.getElementById(item_id_target);
				        // --項目の数だけ繰り返し--
				        while (target_info != null) {
							
							//現在の対象id
							var cur_item_id_target = item_id_base + item_cnt;
							// div要素を作成
							var img_element = document.createElement('div');
							//ラベル取得
							var label = null;
							var this_label = null;
							if (cur_item_id_target !== '') {
							  label = document.querySelector('label[for="' + cur_item_id_target + '"]');
							}
							if (!label) {
							  label = element.closest('label');
							}
							this_label = label ? label.textContent : '';
							//.ラベル取得
							
							//-- 画像の判定 --
							//　　対象の画像パス
							var target_img_pass = '';
							//  ラジオボタン名から判定する
							if(this_label.indexOf('無地') != -1){
								//無地プレート
								target_img_pass = evm + optimg_message_muji;
							}else if(this_label.indexOf('名前が') != -1){
								//おたんじょうびおめでとう(□に名前が入ります)
								target_img_pass = evm + optimg_message_omedetou_plus;
							}else if(this_label.indexOf('たんじょうび') != -1){
								//おたんじょうびおめでとう
								target_img_pass = evm + optimg_message_omedetou_only;
							}
							// 画像パスセット
							img_element.style.width  = '160px';
							img_element.style.height = '100px'; 
							img_element.style.backgroundImage = 'url(' + target_img_pass + ')'; 
							
							//指定要素の前にセット
							target_info.before(img_element);
							
							//次の項目へ
							item_cnt++;
							var tmp_item_id_target = item_id_base + item_cnt;
							target_info = document.getElementById(tmp_item_id_target);
						}
						// --.項目の数だけ繰り返し--
					
						//オプション大タイトルを付ける(メッセージ)
	        			{
	        				var inlbl_id = $opt_id + '_mainlabel';
			        		var innnerLbl = document.getElementById(inlbl_id);
			        		if(innnerLbl != null){
			        				//タグ作成
					        		let lbltag ='<div style="background-color:lightblue; font-size:21px;">';
					        		lbltag = lbltag + '<div class="" style="float:left; width:80%; padding:8px;"><b>メッセージを入力</b></div>';
			        				lbltag = lbltag + '<div style="float:left; width:20%; padding:8px; font-size:16px;"><input type="checkbox" id="chk_message_nosel" name="chk_message_nosel"><label for="chk_message_nosel">不要</label></div>';
			        				lbltag = lbltag + '<div style="clear:both;"></div>';
			        				lbltag = lbltag + '</div>';
			        				
			        				//タグを埋め込む
					        		//innnerLbl.innerHTML = lbltag;
					        		innnerLbl.insertAdjacentHTML('afterend', lbltag);
			        		}
	        			}
					}
	        		//------------------------------------------------------------------------------------------------------------
	        		
	        		//〇包装方法の場合
	        		if (lbl_info.textContent.indexOf('包装方法') != -1) {
		        		//ラジオボタン選択の項目なので先頭に画像をセットさせる
		        		// ラジオボタンの項目はidとして「」のように持っている
		        		var item_id_base = $opt_id + '_';
		        		var item_cnt = 0;//初期値は0番目
		        		// 対象のID名
		        		var item_id_target = $opt_id + '_' + item_cnt;
		        		// あるかチェック
		        		var target_info = document.getElementById(item_id_target);
				        // --項目の数だけ繰り返し--
				        while (target_info != null) {
							
							//現在の対象id
							var cur_item_id_target = item_id_base + item_cnt;
							// div要素を作成
							var img_element = document.createElement('div');
							//ラベル取得
							var label = null;
							var this_label = null;
							if (cur_item_id_target !== '') {
							  label = document.querySelector('label[for="' + cur_item_id_target + '"]');
							}
							if (!label) {
							  label = element.closest('label');
							}
							this_label = label ? label.textContent : '';
							//.ラベル取得
							
							//-- 画像の判定 --
							//　　対象の画像パス
							var target_img_pass = '';
							//  ラジオボタン名から判定する
							if(this_label.indexOf('包装') != -1){
								//包装
								target_img_pass = evm + optimg_housohouho_housou;
							}else if(this_label.indexOf('自宅') != -1){
								//自宅用
								target_img_pass = evm + optimg_housohouho_jitaku;
							}else if(this_label.indexOf('リボン') != -1){
								//リボン
								target_img_pass = evm + optimg_housohouho_ribon;
							}else if(this_label.indexOf('熨斗') != -1){
								//熨斗
								target_img_pass = evm + optimg_housohouho_noshi;
							}
							
							// 画像パスセット
							img_element.style.width  = '100px';
							img_element.style.height = '100px'; 
							img_element.style.backgroundImage = 'url(' + target_img_pass + ')'; 
							
							//指定要素の前にセット
							target_info.before(img_element);
							
							//次の項目へ
							item_cnt++;
							var tmp_item_id_target = item_id_base + item_cnt;
							target_info = document.getElementById(tmp_item_id_target);
						}
						// --.項目の数だけ繰り返し--
					
						//オプション大タイトルを付ける(包装方法)
	        			{
	        				var inlbl_id = $opt_id + '_mainlabel';
			        		var innnerLbl = document.getElementById(inlbl_id);
			        		if(innnerLbl != null){
			        				//タグ作成
					        		let lbltag ='<div style="background-color:lightblue; font-size:21px;">';
					        		lbltag = lbltag + '<div class="" style="float:left; width:80%; padding:8px;"><b>包装方法を選択</b></div>';
			        				lbltag = lbltag + '<div style="float:left; width:20%; padding:8px; font-size:16px;"><input type="checkbox" id="chk_housou_nosel" name="chk_housou_nosel"><label for="chk_housou_nosel">不要</label></div>';
			        				lbltag = lbltag + '<div style="clear:both;"></div>';
			        				lbltag = lbltag + '</div>';
			        				
			        				//タグを埋め込む
					        		//innnerLbl.innerHTML = lbltag;
					        		innnerLbl.insertAdjacentHTML('afterend', lbltag);
			        		}
	        			}
					}
	        		//------------------------------------------------------------------------------------------------------------
	        		
	        		//〇受取店舗の場合
	        		if (lbl_info.textContent.indexOf('店舗') != -1) {
		        		//オプション大タイトルを付ける(受取店舗)
	        			{
	        				var inlbl_id = $opt_id + '_mainlabel';
			        		var innnerLbl = document.getElementById(inlbl_id);
			        		if(innnerLbl != null){
			        				//タグ作成
					        		let lbltag ='<div style="background-color:lightblue; font-size:21px;">';
					        		lbltag = lbltag + '<div class="" style="float:left; width:80%; padding:8px;"><b>受取店舗</b></div>';
			        				lbltag = lbltag + '<div style="float:left; width:20%; padding:8px; font-size:16px;"><input type="checkbox" id="chk_shop_nosel" name="chk_shop_nosel"><label for="chk_shop_nosel">不要</label></div>';
			        				lbltag = lbltag + '<div style="clear:both;"></div>';
			        				lbltag = lbltag + '</div>';
			        				
			        				//タグを埋め込む
					        		//innnerLbl.innerHTML = lbltag;
					        		innnerLbl.insertAdjacentHTML('afterend', lbltag);
			        		}
	        			}
					}
	        		//------------------------------------------------------------------------------------------------------------
	        		
	        		//〇持ち帰りポリ袋の場合
	        		if (lbl_info.textContent.indexOf('持ち帰り') != -1) {
		        		//オプション大タイトルを付ける(持ち帰りポリ袋)
	        			{
	        				var inlbl_id = $opt_id + '_mainlabel';
			        		var innnerLbl = document.getElementById(inlbl_id);
			        		if(innnerLbl != null){
			        				//タグ作成
					        		let lbltag ='<div style="background-color:lightblue; font-size:21px;">';
					        		lbltag = lbltag + '<div class="" style="float:left; width:80%; padding:8px;"><b>持ち帰りポリ袋</b></div>';
			        				lbltag = lbltag + '<div style="float:left; width:20%; padding:8px; font-size:16px;"><input type="checkbox" id="chk_message_nosel" name="chk_message_nosel"><label for="chk_message_nosel">不要</label></div>';
			        				lbltag = lbltag + '<div style="clear:both;"></div>';
			        				lbltag = lbltag + '</div>';
			        				
			        				//タグを埋め込む
					        		//innnerLbl.innerHTML = lbltag;
					        		innnerLbl.insertAdjacentHTML('afterend', lbltag);
			        		}
	        			}
					}
	        		//------------------------------------------------------------------------------------------------------------
	        		
	        	}
	        }
	        
	        
	        //ToDo ケーキならプレート、ギフトならのし　で表示区別してもいいのでは？
	        
	        //プレートオプションがあるなら入力欄を表示
	        if ($optn_select_text.indexOf('プレート') != -1) {
	        	document.getElementById("printname_plate_block").style.display="block";
	        }
	        
	        //のしオプションがあるなら入力欄を表示
	        if ($optn_select_text.indexOf('のし') != -1) {
	        	document.getElementById("printname_noshi_block").style.display="block";
	        }
	    };
	    
	    
	    /**
	     * 該当するオプション入力欄をセットする.
	     */
	    eccube.setOptionText = function($form) {
	        
	        //オプション1
	        var $optn1 = $form.find('select[name=optioncategory_id1]');
	        eccube.getPerOptionText(1, $form, $optn1, 'optioncategory_id1');
	        
	        //オプション2
	        var $optn2 = $form.find('select[name=optioncategory_id2]');
	        eccube.getPerOptionText(2, $form, $optn2, 'optioncategory_id2');
	        
	        //オプション3
	        var $optn3 = $form.find('select[name=optioncategory_id3]');
	        eccube.getPerOptionText(3, $form, $optn3, 'optioncategory_id3');
	        
	        //オプション4
	        var $optn4 = $form.find('select[name=optioncategory_id4]');
	        eccube.getPerOptionText(4, $form, $optn4, 'optioncategory_id4');
	        
	        //オプション5
	        var $optn5 = $form.find('select[name=optioncategory_id5]');
	        eccube.getPerOptionText(5, $form, $optn5, 'optioncategory_id5');
	        
	        //オプション6
	        var $optn6 = $form.find('select[name=optioncategory_id6]');
	        eccube.getPerOptionText(6, $form, $optn6, 'optioncategory_id6');
	        
	        //オプション7
	        var $optn7 = $form.find('select[name=optioncategory_id7]');
	        eccube.getPerOptionText(7, $form, $optn7, 'optioncategory_id7');
	        
	        //オプション8
	        var $optn8 = $form.find('select[name=optioncategory_id8]');
	        eccube.getPerOptionText(8, $form, $optn8, 'optioncategory_id8');
	        
	        //オプション9
	        var $optn9 = $form.find('select[name=optioncategory_id9]');
	        eccube.getPerOptionText(9, $form, $optn9, 'optioncategory_id9');
	        
	        //オプション10
	        var $optn10 = $form.find('select[name=optioncategory_id10]');
	        eccube.getPerOptionText(10, $form, $optn10, 'optioncategory_id10');

	    }
	</script>
    <script>
        eccube.classCategories = {{ class_categories_as_json(Product)|raw }};

        // 規格2に選択肢を割り当てる。
        function fnSetClassCategories(form, classcat_id2_selected) {
            var $form = $(form);
            var product_id = $form.find('input[name=product_id]').val();
            var $sele1 = $form.find('select[name=classcategory_id1]');
            var $sele2 = $form.find('select[name=classcategory_id2]');
            eccube.setClassCategories($form, product_id, $sele1, $sele2, classcat_id2_selected);
        }

        {% if form.classcategory_id2 is defined %}
        fnSetClassCategories(
            $('#form1'), {{ form.classcategory_id2.vars.value|json_encode|raw }}
        );
        {% elseif form.classcategory_id1 is defined %}
        eccube.checkStock($('#form1'), {{ Product.id }}, {{ form.classcategory_id1.vars.value|json_encode|raw }}, null);
        {% endif %}
    </script>
    <script>
        $(function() {
            // bfcache無効化
            $(window).bind('pageshow', function(event) {
                if (event.originalEvent.persisted) {
                    location.reload(true);
                }
            });

            $('.item_visual').slick({
                dots: false,
                arrows: false,
                responsive: [{
                    breakpoint: 768,
                    settings: {
                        dots: true
                    }
                }]
            });

            $('.slideThumb').on('click', function() {
                var index = $(this).attr('data-index');
                $('.item_visual').slick('slickGoTo', index, false);
            })
        });
    </script>
    <script>
        $(function() {
            $('.add-cart').on('click', function(event) {
                {% if form.classcategory_id1 is defined %}
                // 規格1フォームの必須チェック
                if ($('#classcategory_id1').val() == '__unselected' || $('#classcategory_id1').val() == '') {
                    $('#classcategory_id1')[0].setCustomValidity('{{ '項目が選択されていません'|trans }}');
                    return true;
                } else {
                    $('#classcategory_id1')[0].setCustomValidity('');
                }
                {% endif %}

                {% if form.classcategory_id2 is defined %}
                // 規格2フォームの必須チェック
                if ($('#classcategory_id2').val() == '__unselected' || $('#classcategory_id2').val() == '') {
                    $('#classcategory_id2')[0].setCustomValidity('{{ '項目が選択されていません'|trans }}');
                    return true;
                } else {
                    $('#classcategory_id2')[0].setCustomValidity('');
                }
                {% endif %}

                // 個数フォームのチェック
                if ($('#quantity').val() < 1) {
                    $('#quantity')[0].setCustomValidity('{{ '1以上で入力してください。'|trans }}');
                    return true;
                } else {
                    $('#quantity')[0].setCustomValidity('');
                }
                
                //追加価格のオプションがあれば追加価格をセットする.
                eccube.checkOptionPrice($('#form1'));

                event.preventDefault();
                $form = $('#form1');
                $.ajax({
                    url: $form.attr('action'),
                    type: $form.attr('method'),
                    data: $form.serialize(),
                    dataType: 'json',
                    beforeSend: function(xhr, settings) {
                        // Buttonを無効にする
                        $('.add-cart').prop('disabled', true);
                    }
                }).done(function(data) {
                    // レスポンス内のメッセージをalertで表示
                    $.each(data.messages, function() {
                        $('#ec-modal-header').html(this);
                    });

                    $('.ec-modal').show()

                    // カートブロックを更新する
                    $.ajax({
                        url: "{{ url('block_cart') }}",
                        type: 'GET',
                        dataType: 'html'
                    }).done(function(html) {
                        $('.ec-headerRole__cart').html(html);
                    });
                }).fail(function(data) {
                    alert('{{ 'カートへの追加に失敗しました。'|trans }}');
                }).always(function(data) {
                    // Buttonを有効にする
                    $('.add-cart').prop('disabled', false);
                });
            });
        });
        
        //詳細を指定する
        $('.select-options').on('click', function() {
            //詳細ブロックを表示する
            var syousai_sitei_id = 'blk_syousai_sitei';
    		if(document.getElementById(syousai_sitei_id) != null){
				document.getElementById(syousai_sitei_id).style.display="block";
			}
            
            //商品メインブロックを非表示にする
            var syohin_main_id = 'blk_syohin_main';
    		if(document.getElementById(syohin_main_id) != null){
				document.getElementById(syohin_main_id).style.display="none";
			}
			
			//ページトップへ移動
			document.body.scrollTop = document.documentElement.scrollTop = 0;
        });
        
        //前に戻る
        $('.back-main').on('click', function() {
            //詳細ブロックを非表示にする
            var syousai_sitei_id = 'blk_syousai_sitei';
    		if(document.getElementById(syousai_sitei_id) != null){
				document.getElementById(syousai_sitei_id).style.display="none";
			}
            
            //商品メインブロックを表示する
            var syohin_main_id = 'blk_syohin_main';
    		if(document.getElementById(syohin_main_id) != null){
				document.getElementById(syohin_main_id).style.display="block";
			}
			
			//ページトップへ移動
			document.body.scrollTop = document.documentElement.scrollTop = 0;
        });
        
        
        //オプションの数字が入力されたとき
        //詳細を指定する
        /*
        $('.select-options').on('click', function() {
            //event.preventDefault();
            //ToDo 詳細エリアにスクロールする
            alert('go scroll!');
        });
        */
        
        $('input[type="number"]').bind('keyup mouseup', function () {
		    
		    //数字入力された項目のidを取得
		    $this_item_id = this.id;
		    
		    $this_item_str = $this_item_id.toString();
		    
		    //意図的にテキスト入力されたラジオボタンのチェックを入れる
		    //　(オプション項目の場合のみ実施する)
		    if($this_item_str.indexOf('optioncategory_id') != -1){
			    //入力された数字も取得　なければしない
			    const elem = document.getElementById($this_item_id);
				
				if(elem != null){
					//存在するなら処理実施
					//取れる値(数字)をセット
					var num = elem.value;
				    
				    //数字セットされてるなら該当のラジオボタンをチェックする
				    if(num.length < 1)
				    {
				    	//数字セットされてないなら0番目(なし)をチェックする
				    	num = 0;
				    }
				    
				    //ラジオボタン名は「_num」を除いた名前
				    $this_item_name = $this_item_str.replace('_num', '')
				    
				    //選択番号
					var selnum = num;
					
					// 要素名セット
					var ele_name = $this_item_name + '_' + selnum;
					// form要素を取得
					var rdo_ele = document.getElementById( ele_name ) ;

					// 該当番号の要素を選択状態にする
					if(rdo_ele != null){
						rdo_ele.checked = true;
					}
				}
			}
		});
		
        
        
    </script>
{% endblock %}

{% block main %}
    <div class="ec-productRole">
        <div class="ec-grid2">
            <div class="ec-grid2__cell">
                <div class="ec-sliderItemRole">

                    <div class="item_visual">
                        {% for ProductImage in Product.ProductImage %}
                            <div class="slide-item"><img src="{{ asset(ProductImage, 'save_image') }}"></div>
                        {% else %}
                            <div class="slide-item"><img src="{{ asset(''|no_image_product, 'save_image') }}"/></div>
                        {% endfor %}
                    </div>
                    <div class="item_nav">
                        {% for ProductImage in Product.ProductImage %}
                            <div class="slideThumb" data-index="{{ loop.index0 }}"><img src="{{ asset(ProductImage, 'save_image') }}"></div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        
        <form action="{{ url('product_add_cart', {id:Product.id}) }}" method="post" id="form1" name="form1">
                    
            <div class="ec-grid2__cell" style="width:100%;">
                <div class="ec-productRole__profile">
                    {# 商品名 #}
                    <div class="ec-productRole__title">
                        <h2 class="ec-headingTitle"><b>{{ Product.name }}</b></h2>
                    </div>
                    {# 商品説明 #}
                    <div class="ec-productRole__description" style="padding-top:10px;">{{ Product.description_detail|raw|nl2br }}
                    {# タグ #}
                    <!--
                    <ul class="ec-productRole__tags">
                        {% for Tag in Product.Tags %}
                            <li class="ec-productRole__tag tag_{{ Tag.id }}">{{ Tag }}</li>
                        {% endfor %}
                    </ul>
                    -->
                    
                    {# 商品コード #}
                    <!--
                    {% if Product.code_min is not empty %}
                        <div class="ec-productRole__code">
                            {{ '商品コード'|trans }}： <span class="product-code-default">{{ Product.code_min }}{% if Product.code_min != Product.code_max %} ～ {{ Product.code_max }}{% endif %}</span>
                        </div>
                    {% endif %}
                    -->
                    {# 関連カテゴリ #}
                    <!--
                    {% if Product.ProductCategories is not empty %}
                        <div class="ec-productRole__category">
                            <div>{{ '関連カテゴリ'|trans }}</div>
                            {% for ProductCategory in Product.ProductCategories %}
                                <ul>
                                    <li>
                                        {% for Category in ProductCategory.Category.path %}
                                            <a href="{{ url('product_list') }}?category_id={{ Category.id }}">{{ Category.name }}</a> {%- if loop.last == false %}
                                            <span>＞</span>{% endif -%}
                                        {% endfor %}
                                    </li>
                                </ul>
                            {% endfor %}
                        </div>
                    {% endif %}
                    -->
                        {% if Product.stock_find %}
                         <div id="blk_syohin_main" style="display:block;">   
                            <!-- サイズ -->
                            {% if form.classcategory_id1 is defined %}
                                <div class="">
                                    {{ form_widget(form.classcategory_id1) }}
                                    {{ form_errors(form.classcategory_id1) }}
                                </div>
                                {% if form.classcategory_id2 is defined %}
                                    <div class="">
                                        {{ form_widget(form.classcategory_id2) }}
                                        {{ form_errors(form.classcategory_id2) }}
                                    </div>
                                {% endif %}
                            {% endif %}
                            
                            <!-- 個数(サイズのみ) -->
                            <div class="ec-numberInput">
                            	<span>{{ '個数:'|trans }}</span>
                                {{ form_widget(form.quantity) }}
                                {{ form_errors(form.quantity) }}
                                <span>{{ '個'|trans }}</span>
                            </div>
                            
                            <!-- 金額(サイズのみ) -->
                            
                            <!-- 詳細を指定するボタン -->
                            <div class="ec-productRole__btn" style="padding-top:10px;width:100%;">
                                <button type="button" class="ec-blockBtn--action select-options" style="outline:none; background: url({{ asset('assets/img/btn_syousai.png') }}) no-repeat scroll 0 0; border-color:#fff;min-height:100px; height:auto;">
                                </button>
                            </div>
                            
                            
                            <!-- 自由入力 -->
					        {% if Product.freearea %}
					             <div class="ec-productRole__description">
					                 {{ include(template_from_string(Product.freearea)) }}
					            </div>
					        {% endif %}
					     </div>
                         <!-- .商品メインブロック -->
                        {% else %}
                            <div class="ec-productRole__btn">
                                <button type="button" class="ec-blockBtn--action" disabled="disabled">
                                    {{ 'ただいま品切れ中です。'|trans }}
                                </button>
                            </div>
                        {% endif %} 
                            
                            
                            
                        <!----- 【詳細を指定】ブロック ----->
                        {% if Product.stock_find %}
                         <div id="blk_syousai_sitei" style="display:none;">
                            {# 詳細を指定する #}
                            <div class="ec-productRole__profile" style="width:100%; padding-top:100px;">
			                    <div style="margin-left:auto; margin-right:auto; background: url({{ asset('assets/img/mark_write_sentence.png') }}) no-repeat scroll 0 0; border-color:#fff;min-height:150px; height:auto; outline:none;">
	                            </div>
			                </div>
                            
                            <!-- 詳細指定エリア -->
                            <div id="detail_area"></div>
                            
                            
		                    <!-- オプション一覧 -->
		                    <div style="position: relative;border-radius: 2px;background-color: #fff;">
		                                
			                            <div class="ec-productRole__actions">
			                                
			                                <!-- メッセージを含むか判定 -->
			                                {% set exist_message = false %}
			                                <!-- 熨斗を含むか判定 -->
			                                {% set exist_noshi = false %}
			                                
			                                <!-- オプション -->
			                                {% if form.optioncategory_id1 is defined %}
			                                	<!-- 大オプションラベル -->
			                                    <div id="optioncategory_id1_mainlabel"></div>
			                                    <!-- 文言 -->
												<div id="optioncategory_id1_mongon"></div>
			                                    
			                                    <!-- ブロック -->
												<div id="optioncategory_id1_blk">
				                                    <!-- オプション画像 -->
				                                    <div id="optioncategory_id1_gazou"></div>
				                                    <!-- 入力エリア -->
				                                	<div class="">
				                                        <!-- 登録オプション名 -->
				                                        <p id="optioncategory_id1_lbl" style="display:none;">
				                                    		{{ form_label(form.optioncategory_id1) }}
				                                        </p>
				                                        <!-- オプション項目名 -->
														<div style="display:block;">
															<div id="optioncategory_id1_koumokumei"></div>
						                                    <!-- アイコン -->
						                                    <div id="optioncategory_id1_icon"></div>
						                                </div>
				                                        <!-- 特別な入力欄 -->			                                        
				                                        <div id="optioncategory_id1_nyuryoku" style="display:none;">
															<div class="ec-numberInput">
																<span>本数:</span>
																<input type="number" id="optioncategory_id1_num" name="optioncategory_id1_num" required="required" min="1" max="10" maxlength="2" class="form-control" value="">
																<span>本</span>
															</div>
				                                        </div>
				                                        
				                                        <div id="optioncategory_id1_wdg" style="display:block;">
				                                        	{{ form_widget(form.optioncategory_id1) }}
				                                        </div>
				                                        
				                                        
				                                        {% if 'メッセージ' in form_label(form.optioncategory_id1) %}
				                                        	<!--    名入れ(プレート)    -->
							                                {% if form.printname_plate is defined %}
								                                <div id="printname_plate_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_plate) }}
									                                    {{ form_errors(form.printname_plate) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_plate" name="printname_plate" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(プレート)    -->
				                                        	{% set exist_message = true %}
				                                        {% endif %}
				                                        {% if '上書き' in form_label(form.optioncategory_id1) %}
				                                        	<!--    名入れ(熨斗)    -->
							                                {% if form.printname_noshi is defined %}
								                                <div id="printname_noshi_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_noshi) }}
									                                    {{ form_errors(form.printname_noshi) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_noshi" name="printname_noshi" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(熨斗)    -->
				                                        	{% set exist_noshi = true %}
				                                        {% endif %}
				                                        
				                                        {{ form_errors(form.optioncategory_id1) }}
				                                    </div>
				                            	</div>
			                                {% endif %}
			                                {% if form.optioncategory_id2 is defined %}
			                                    <!-- 大オプションラベル -->
			                                    <div id="optioncategory_id2_mainlabel"></div>
			                                    <!-- 文言 -->
												<div id="optioncategory_id2_mongon"></div>
												
												<!-- ブロック -->
												<div id="optioncategory_id2_blk">
				                                    <!-- オプション画像 -->
				                                    <div id="optioncategory_id2_gazou"></div>
				                                    <!-- 入力エリア -->
				                                    <div class="">
				                                    	<!-- 登録オプション名 -->
				                                    	<p id="optioncategory_id2_lbl" style="display:none;">
				                                    		{{ form_label(form.optioncategory_id2) }}
				                                        </p>
				                                        <!-- オプション項目名 -->
														<div style="display:block;">
															<div id="optioncategory_id2_koumokumei"></div>
						                                    <!-- アイコン -->
						                                    <div id="optioncategory_id2_icon"></div>
						                                </div>
				                                        <!-- 特別な入力欄 -->			                                        
				                                        <div id="optioncategory_id2_nyuryoku" style="display:none;">
				                                        	<div class="ec-numberInput">
																<span>本数:</span>
																<input type="number" id="optioncategory_id2_num" name="optioncategory_id2_num" required="required" min="1" max="10" maxlength="2" class="form-control" value="">
																<span>本</span>
															</div>
				                                        </div>
				                                        
				                                        <div id="optioncategory_id2_wdg" style="display:block;">
				                                        	{{ form_widget(form.optioncategory_id2) }}
				                                        </div>
				                                        
				                                        {% if 'メッセージ' in form_label(form.optioncategory_id2) %}
				                                        	<!--    名入れ(プレート)    -->
							                                {% if form.printname_plate is defined %}
								                                <div id="printname_plate_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_plate) }}
									                                    {{ form_errors(form.printname_plate) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_plate" name="printname_plate" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(プレート)    -->
				                                        	{% set exist_message = true %}
				                                        {% endif %}
				                                        {% if '上書き' in form_label(form.optioncategory_id2) %}
				                                        	<!--    名入れ(熨斗)    -->
							                                {% if form.printname_noshi is defined %}
								                                <div id="printname_noshi_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_noshi) }}
									                                    {{ form_errors(form.printname_noshi) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_noshi" name="printname_noshi" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(熨斗)    -->
				                                        	{% set exist_noshi = true %}
				                                        {% endif %}
				                                        
				                                        {{ form_errors(form.optioncategory_id2) }}
				                                    </div>
				                            	</div>
			                                {% endif %}
			                                {% if form.optioncategory_id3 is defined %}
			                                    <!-- 大オプションラベル -->
			                                    <div id="optioncategory_id3_mainlabel"></div>
			                                    <!-- 文言 -->
												<div id="optioncategory_id3_mongon"></div>
			                                    
			                                    <!-- ブロック -->
												<div id="optioncategory_id3_blk">
					                                <!-- オプション画像 -->
				                                    <div id="optioncategory_id3_gazou"></div>
				                                    <!-- 入力エリア -->
				                                    <div class="">
				                                    	<!-- 登録オプション名 -->
				                                    	<p id="optioncategory_id3_lbl" style="display:none;">
				                                    		{{ form_label(form.optioncategory_id3) }}
				                                        </p>
				                                        <!-- オプション項目名 -->
														<div style="display:block;">
															<div id="optioncategory_id3_koumokumei"></div>
						                                    <!-- アイコン -->
						                                    <div id="optioncategory_id3_icon"></div>
						                                </div>
				                                        <!-- 特別な入力欄 -->			                                        
				                                        <div id="optioncategory_id3_nyuryoku" style="display:none;">
				                                        	<div class="ec-numberInput">
																<span>本数:</span>
																<input type="number" id="optioncategory_id3_num" name="optioncategory_id3_num" required="required" min="1" max="10" maxlength="2" class="form-control" value="">
																<span>本</span>
															</div>
				                                        </div>
				                                        
				                                        <div id="optioncategory_id3_wdg" style="display:block;">
				                                        	{{ form_widget(form.optioncategory_id3) }}
				                                        </div>
				                                        
				                                        {% if 'メッセージ' in form_label(form.optioncategory_id3) %}
				                                        	<!--    名入れ(プレート)    -->
							                                {% if form.printname_plate is defined %}
								                                <div id="printname_plate_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_plate) }}
									                                    {{ form_errors(form.printname_plate) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_plate" name="printname_plate" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(プレート)    -->
				                                        	{% set exist_message = true %}
				                                        {% endif %}
				                                        {% if '上書き' in form_label(form.optioncategory_id3) %}
				                                        	<!--    名入れ(熨斗)    -->
							                                {% if form.printname_noshi is defined %}
								                                <div id="printname_noshi_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_noshi) }}
									                                    {{ form_errors(form.printname_noshi) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_noshi" name="printname_noshi" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(熨斗)    -->
				                                        	{% set exist_noshi = true %}
				                                        {% endif %}
				                                        
				                                        {{ form_errors(form.optioncategory_id3) }}
				                                    </div>
				                            	</div>
			                                {% endif %}
			                                {% if form.optioncategory_id4 is defined %}
			                                    <!-- 大オプションラベル -->
			                                    <div id="optioncategory_id4_mainlabel"></div>
			                                    <!-- 文言 -->
												<div id="optioncategory_id4_mongon"></div>
			                                    
			                                    <!-- ブロック -->
												<div id="optioncategory_id4_blk">
						                            <!-- オプション画像 -->
				                                    <div id="optioncategory_id4_gazou"></div>
				                                    <!-- 入力エリア -->
				                                    <div class="">
				                                    	<!-- 登録オプション名 -->
				                                    	<p id="optioncategory_id4_lbl" style="display:none;">
				                                    		{{ form_label(form.optioncategory_id4) }}
				                                        </p>
				                                        <!-- オプション項目名 -->
														<div style="display:block;">
															<div id="optioncategory_id4_koumokumei"></div>
						                                    <!-- アイコン -->
						                                    <div id="optioncategory_id4_icon"></div>
						                                </div>
				                                        <!-- 特別な入力欄 -->			                                        
				                                        <div id="optioncategory_id4_nyuryoku" style="display:none;">
				                                        	<div class="ec-numberInput">
																<span>本数:</span>
																<input type="number" id="optioncategory_id4_num" name="optioncategory_id4_num" required="required" min="1" max="10" maxlength="2" class="form-control" value="">
																<span>本</span>
															</div>
				                                        </div>
				                                        
				                                        <div id="optioncategory_id4_wdg" style="display:block;">
				                                        	{{ form_widget(form.optioncategory_id4) }}
				                                        </div>
				                                        
				                                        
				                                        {% if 'メッセージ' in form_label(form.optioncategory_id4) %}
				                                        	<!--    名入れ(プレート)    -->
							                                {% if form.printname_plate is defined %}
								                                <div id="printname_plate_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_plate) }}
									                                    {{ form_errors(form.printname_plate) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_plate" name="printname_plate" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(プレート)    -->
				                                        	{% set exist_message = true %}
				                                        {% endif %}
				                                        {% if '上書き' in form_label(form.optioncategory_id4) %}
				                                        	<!--    名入れ(熨斗)    -->
							                                {% if form.printname_noshi is defined %}
								                                <div id="printname_noshi_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_noshi) }}
									                                    {{ form_errors(form.printname_noshi) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_noshi" name="printname_noshi" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(熨斗)    -->
				                                        	{% set exist_noshi = true %}
				                                        {% endif %}
				                                        
				                                        {{ form_errors(form.optioncategory_id4) }}
				                                    </div>
				                            	</div>
			                                {% endif %}
			                                {% if form.optioncategory_id5 is defined %}
			                                    <!-- 大オプションラベル -->
			                                    <div id="optioncategory_id5_mainlabel"></div>
			                                    <!-- 文言 -->
												<div id="optioncategory_id5_mongon"></div>
			                                    
			                                    <!-- ブロック -->
												<div id="optioncategory_id5_blk">
				                                    <!-- オプション画像 -->
				                                    <div id="optioncategory_id5_gazou"></div>
				                                    <!-- 入力エリア -->
				                                    <div class="">
				                                        <!-- 登録オプション名 -->
				                                        <p id="optioncategory_id5_lbl" style="display:none;">
				                                    		{{ form_label(form.optioncategory_id5) }}
				                                        </p>
				                                        <!-- オプション項目名 -->
														<div style="display:block;">
															<div id="optioncategory_id5_koumokumei"></div>
						                                    <!-- アイコン -->
						                                    <div id="optioncategory_id5_icon"></div>
						                                </div>
				                                        <!-- 特別な入力欄 -->			                                        
				                                        <div id="optioncategory_id5_nyuryoku" style="display:none;">
				                                        	<div class="ec-numberInput">
																<span>本数:</span>
																<input type="number" id="optioncategory_id5_num" name="optioncategory_id5_num" required="required" min="1" max="10" maxlength="2" class="form-control" value="">
																<span>本</span>
															</div>
				                                        </div>
				                                        
				                                        <div id="optioncategory_id5_wdg" style="display:block;">
				                                        	{{ form_widget(form.optioncategory_id5) }}
				                                        </div>
				                                        
				                                        {% if 'メッセージ' in form_label(form.optioncategory_id5) %}
				                                        	<!--    名入れ(プレート)    -->
							                                {% if form.printname_plate is defined %}
								                                <div id="printname_plate_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_plate) }}
									                                    {{ form_errors(form.printname_plate) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_plate" name="printname_plate" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(プレート)    -->
				                                        	{% set exist_message = true %}
				                                        {% endif %}
				                                        {% if '上書き' in form_label(form.optioncategory_id5) %}
				                                        	<!--    名入れ(熨斗)    -->
							                                {% if form.printname_noshi is defined %}
								                                <div id="printname_noshi_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_noshi) }}
									                                    {{ form_errors(form.printname_noshi) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_noshi" name="printname_noshi" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(熨斗)    -->
				                                        	{% set exist_noshi = true %}
				                                        {% endif %}
				                                        
				                                        {{ form_errors(form.optioncategory_id5) }}
				                                    </div>
				                            	</div>
			                                {% endif %}
			                                {% if form.optioncategory_id6 is defined %}
			                                    <!-- 大オプションラベル -->
			                                    <div id="optioncategory_id6_mainlabel"></div>
			                                    <!-- 文言 -->
												<div id="optioncategory_id6_mongon"></div>
			                                    
			                                    <!-- ブロック -->
												<div id="optioncategory_id6_blk">
					                                <!-- オプション画像 -->
				                                    <div id="optioncategory_id6_gazou"></div>
				                                    <!-- 入力エリア -->
				                                    <div class="">
				                                    	<!-- 登録オプション名 -->
				                                    	<p id="optioncategory_id6_lbl" style="display:none;">
				                                    		{{ form_label(form.optioncategory_id6) }}
				                                        </p>
				                                        <!-- オプション項目名 -->
														<div style="display:block;">
															<div id="optioncategory_id6_koumokumei"></div>
						                                    <!-- アイコン -->
						                                    <div id="optioncategory_id6_icon"></div>
						                                </div>
				                                        <!-- 特別な入力欄 -->			                                        
				                                        <div id="optioncategory_id6_nyuryoku" style="display:none;">
				                                        	<div class="ec-numberInput">
																<span>本数:</span>
																<input type="number" id="optioncategory_id6_num" name="optioncategory_id6_num" required="required" min="1" max="10" maxlength="2" class="form-control" value="">
																<span>本</span>
															</div>
				                                        </div>
				                                        
				                                        <div id="optioncategory_id6_wdg" style="display:block;">
				                                        	{{ form_widget(form.optioncategory_id6) }}
				                                        </div>
				                                        
				                                        {% if 'メッセージ' in form_label(form.optioncategory_id6) %}
				                                        	<!--    名入れ(プレート)    -->
							                                {% if form.printname_plate is defined %}
								                                <div id="printname_plate_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_plate) }}
									                                    {{ form_errors(form.printname_plate) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_plate" name="printname_plate" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(プレート)    -->
				                                        	{% set exist_message = true %}
				                                        {% endif %}
				                                        {% if '上書き' in form_label(form.optioncategory_id6) %}
				                                        	<!--    名入れ(熨斗)    -->
							                                {% if form.printname_noshi is defined %}
								                                <div id="printname_noshi_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_noshi) }}
									                                    {{ form_errors(form.printname_noshi) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_noshi" name="printname_noshi" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(熨斗)    -->
				                                        	{% set exist_noshi = true %}
				                                        {% endif %}
				                                        
				                                        {{ form_errors(form.optioncategory_id6) }}
				                                    </div>
				                            	</div>
			                                {% endif %}
			                                {% if form.optioncategory_id7 is defined %}
			                                    <!-- 大オプションラベル -->
			                                    <div id="optioncategory_id7_mainlabel"></div>
			                                    <!-- 文言 -->
												<div id="optioncategory_id7_mongon"></div>
			                                    
			                                    <!-- ブロック -->
												<div id="optioncategory_id7_blk">
						                            <!-- オプション画像 -->
				                                    <div id="optioncategory_id7_gazou"></div>
				                                    <!-- 入力エリア -->
				                                    <div class="">
				                                    	<!-- 登録オプション名 -->
				                                    	<p id="optioncategory_id7_lbl" style="display:none;">
				                                    		{{ form_label(form.optioncategory_id7) }}
				                                        </p>
				                                        <!-- オプション項目名 -->
														<div style="display:block;">
															<div id="optioncategory_id7_koumokumei"></div>
						                                    <!-- アイコン -->
						                                    <div id="optioncategory_id7_icon"></div>
						                                </div>
				                                        <!-- 特別な入力欄 -->			                                        
				                                        <div id="optioncategory_id7_nyuryoku" style="display:none;">
				                                        	<div class="ec-numberInput">
																<span>本数:</span>
																<input type="number" id="optioncategory_id7_num" name="optioncategory_id7_num" required="required" min="1" max="10" maxlength="2" class="form-control" value="">
																<span>本</span>
															</div>
				                                        </div>
				                                        
				                                        <div id="optioncategory_id7_wdg" style="display:block;">
				                                        	{{ form_widget(form.optioncategory_id7) }}
				                                        </div>
				                                        
				                                        {% if 'メッセージ' in form_label(form.optioncategory_id7) %}
				                                        	<!--    名入れ(プレート)    -->
							                                {% if form.printname_plate is defined %}
								                                <div id="printname_plate_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_plate) }}
									                                    {{ form_errors(form.printname_plate) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_plate" name="printname_plate" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(プレート)    -->
				                                        	{% set exist_message = true %}
				                                        {% endif %}
				                                        {% if '上書き' in form_label(form.optioncategory_id7) %}
				                                        	<!--    名入れ(熨斗)    -->
							                                {% if form.printname_noshi is defined %}
								                                <div id="printname_noshi_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_noshi) }}
									                                    {{ form_errors(form.printname_noshi) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_noshi" name="printname_noshi" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(熨斗)    -->
				                                        	{% set exist_noshi = true %}
				                                        {% endif %}
				                                        
				                                        {{ form_errors(form.optioncategory_id7) }}
				                                    </div>
				                            	</div>
			                                {% endif %}
			                                {% if form.optioncategory_id8 is defined %}
			                                    <!-- 大オプションラベル -->
			                                    <div id="optioncategory_id8_mainlabel"></div>
			                                    <!-- 文言 -->
												<div id="optioncategory_id8_mongon"></div>
			                                    
			                                    <!-- ブロック -->
												<div id="optioncategory_id8_blk">
							                        <!-- オプション画像 -->
				                                    <div id="optioncategory_id8_gazou"></div>
				                                    <!-- 入力エリア -->
				                                    <div class="">
				                                    	<!-- 登録オプション名 -->
				                                    	<p id="optioncategory_id8_lbl" style="display:none;">
				                                    		{{ form_label(form.optioncategory_id8) }}
				                                        </p>
				                                        <!-- オプション項目名 -->
														<div style="display:block;">
															<div id="optioncategory_id8_koumokumei"></div>
						                                    <!-- アイコン -->
						                                    <div id="optioncategory_id8_icon"></div>
						                                </div>
				                                        <!-- 特別な入力欄 -->			                                        
				                                        <div id="optioncategory_id8_nyuryoku" style="display:none;">
				                                        	<div class="ec-numberInput">
																<span>本数:</span>
																<input type="number" id="optioncategory_id8_num" name="optioncategory_id8_num" required="required" min="1" max="10" maxlength="2" class="form-control" value="">
																<span>本</span>
															</div>
				                                        </div>
				                                        
				                                        <div id="optioncategory_id8_wdg" style="display:block;">
				                                        	{{ form_widget(form.optioncategory_id8) }}
				                                        </div>
				                                        
				                                        {% if 'メッセージ' in form_label(form.optioncategory_id8) %}
				                                        	<!--    名入れ(プレート)    -->
							                                {% if form.printname_plate is defined %}
								                                <div id="printname_plate_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_plate) }}
									                                    {{ form_errors(form.printname_plate) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_plate" name="printname_plate" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(プレート)    -->
				                                        	{% set exist_message = true %}
				                                        {% endif %}
				                                        {% if '上書き' in form_label(form.optioncategory_id8) %}
				                                        	<!--    名入れ(熨斗)    -->
							                                {% if form.printname_noshi is defined %}
								                                <div id="printname_noshi_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_noshi) }}
									                                    {{ form_errors(form.printname_noshi) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_noshi" name="printname_noshi" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(熨斗)    -->
				                                        	{% set exist_noshi = true %}
				                                        {% endif %}
				                                        
				                                        {{ form_errors(form.optioncategory_id8) }}
				                                    </div>
				                            	</div>
			                                {% endif %}
			                                {% if form.optioncategory_id9 is defined %}
			                                    <!-- 大オプションラベル -->
			                                    <div id="optioncategory_id9_mainlabel"></div>
			                                    <!-- 文言 -->
												<div id="optioncategory_id9_mongon"></div>
			                                    
			                                    <!-- ブロック -->
												<div id="optioncategory_id9_blk">
								                    <!-- オプション画像 -->
				                                    <div id="optioncategory_id9_gazou"></div>
				                                    <!-- 入力エリア -->
				                                    <div class="">
				                                        <!-- 登録オプション名 -->
				                                        <p id="optioncategory_id9_lbl" style="display:none;">
				                                    		{{ form_label(form.optioncategory_id9) }}
				                                        </p>
				                                        <!-- オプション項目名 -->
														<div style="display:block;">
															<div id="optioncategory_id9_koumokumei"></div>
						                                    <!-- アイコン -->
						                                    <div id="optioncategory_id9_icon"></div>
						                                </div>
				                                        <!-- 特別な入力欄 -->			                                        
				                                        <div id="optioncategory_id9_nyuryoku" style="display:none;">
				                                        	<div class="ec-numberInput">
																<span>本数:</span>
																<input type="number" id="optioncategory_id9_num" name="optioncategory_id9_num" required="required" min="1" max="10" maxlength="2" class="form-control" value="">
																<span>本</span>
															</div>
				                                        </div>
				                                        
				                                        <div id="optioncategory_id9_wdg" style="display:block;">
				                                        	{{ form_widget(form.optioncategory_id9) }}
				                                        </div>
				                                        
				                                        {% if 'メッセージ' in form_label(form.optioncategory_id9) %}
				                                        	<!--    名入れ(プレート)    -->
							                                {% if form.printname_plate is defined %}
								                                <div id="printname_plate_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_plate) }}
									                                    {{ form_errors(form.printname_plate) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_plate" name="printname_plate" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(プレート)    -->
				                                        	{% set exist_message = true %}
				                                        {% endif %}
				                                        {% if '上書き' in form_label(form.optioncategory_id9) %}
				                                        	<!--    名入れ(熨斗)    -->
							                                {% if form.printname_noshi is defined %}
								                                <div id="printname_noshi_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_noshi) }}
									                                    {{ form_errors(form.printname_noshi) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_noshi" name="printname_noshi" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(熨斗)    -->
				                                        	{% set exist_noshi = true %}
				                                        {% endif %}
				                                        
				                                        {{ form_errors(form.optioncategory_id9) }}
				                                    </div>
				                            	</div>
			                                {% endif %}
			                                {% if form.optioncategory_id10 is defined %}
			                                    <!-- 大オプションラベル -->
			                                    <div id="optioncategory_id10_mainlabel"></div>
			                                    <!-- 文言 -->
												<div id="optioncategory_id10_mongon"></div>
			                                    
			                                    <!-- ブロック -->
												<div id="optioncategory_id10_blk">
									                <!-- オプション画像 -->
				                                    <div id="optioncategory_id10_gazou"></div>
				                                    <!-- 入力エリア -->
				                                    <div class="">
				                                    	<!-- 登録オプション名 -->
				                                    	<p id="optioncategory_id10_lbl" style="display:none;">
				                                    		{{ form_label(form.optioncategory_id10) }}
				                                        </p>
				                                        <!-- オプション項目名 -->
														<div style="display:block;">
															<div id="optioncategory_id10_koumokumei"></div>
						                                    <!-- アイコン -->
						                                    <div id="optioncategory_id10_icon"></div>
						                                </div>
				                                        <!-- 特別な入力欄 -->			                                        
				                                        <div id="optioncategory_id10_nyuryoku" style="display:none;">
				                                        	<div class="ec-numberInput">
																<span>本数:</span>
																<input type="number" id="optioncategory_id10_num" name="optioncategory_id10_num" required="required" min="1" max="10" maxlength="2" class="form-control" value="">
																<span>本</span>
															</div>
				                                        </div>
				                                        
				                                        <div id="optioncategory_id10_wdg" style="display:block;">
				                                        	{{ form_widget(form.optioncategory_id10) }}
				                                        </div>
				                                        
				                                        {% if 'メッセージ' in form_label(form.optioncategory_id10) %}
				                                        	<!--    名入れ(プレート)    -->
							                                {% if form.printname_plate is defined %}
								                                <div id="printname_plate_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_plate) }}
									                                    {{ form_errors(form.printname_plate) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_plate" name="printname_plate" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(プレート)    -->
				                                        	{% set exist_message = true %}
				                                        {% endif %}
				                                        {% if '上書き' in form_label(form.optioncategory_id10) %}
				                                        	<!--    名入れ(熨斗)    -->
							                                {% if form.printname_noshi is defined %}
								                                <div id="printname_noshi_block" style="display:block;">
									                                <div class="ec-rectHeading" style="font-size:14px;">
													                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
													                </div>
									                                <div class="ec-input">
													                    {{ form_widget(form.printname_noshi) }}
									                                    {{ form_errors(form.printname_noshi) }}
													                </div>
												                </div>
												            {% else %}
												            	<div class="ec-input" style="display:none;">
												                    <textarea id="printname_noshi" name="printname_noshi" class="form-control"></textarea>
								                                    
												                </div>
											                {% endif %}
							                                <!--    .名入れ(熨斗)    -->
				                                        	{% set exist_noshi = true %}
				                                        {% endif %}
				                                        
				                                        {{ form_errors(form.optioncategory_id10) }}
				                                    </div>
				                            	</div>
			                                {% endif %}
			                                
			                                <!--    名入れ(プレート)    -->
			                                {% if form.printname_plate is defined and exist_message == false %}
				                                <div id="printname_plate_block" style="display:none;">
					                                <div class="ec-rectHeading" style="font-size:14px;">
									                    {{ 'お名前おつけできます。「お誕生日おめでとう」以外の場合も入力してください。'|trans }}
									                </div>
					                                <div class="ec-input">
									                    {{ form_widget(form.printname_plate) }}
					                                    {{ form_errors(form.printname_plate) }}
									                </div>
								                </div>
							                {% endif %}
			                                <!--    .名入れ(プレート)    -->
			                                
			                                <!--    名入れ(熨斗)    -->
			                                {% if form.printname_noshi is defined and exist_noshi == false %}
				                                <div id="printname_noshi_block" style="display:none;">
					                                <div class="ec-rectHeading" style="font-size:14px;">
									                    {{ '熨斗の名入れ希望はこちらへ（ふりがなもご記入頂ければ対応可能です）'|trans }}
									                </div>
					                                <div class="ec-input">
									                    {{ form_widget(form.printname_noshi) }}
					                                    {{ form_errors(form.printname_noshi) }}
									                </div>
								                </div>
							                {% endif %}
			                                <!--    .名入れ(熨斗)    -->
			                                
			                                <!-- .オプション -->
			                                
			                                
			                                
			                                {# 定価 #}
						                    {% if Product.hasProductClass -%}
						                        <div class="ec-productRole__priceRegular">
						                            {% if Product.getPrice01Min is not null and Product.getPrice01IncTaxMin == Product.getPrice01IncTaxMax %}
						                                <span class="ec-productRole__priceRegularPrice">{{ '定価'|trans }}：<span class="price01-default">{{ Product.getPrice01IncTaxMin|price }}</span></span>
						                                <span class="ec-productRole__priceRegularTax">{{ '(税込)'|trans }}</span>
						                            {% elseif Product.getPrice01Min is not null and Product.getPrice01Max is not null %}
						                                <span class="ec-productRole__priceRegularPrice">{{ '定価'|trans }}：<span class="price01-default">{{ Product.getPrice01IncTaxMin|price }}～ {{ Product.getPrice01IncTaxMax|price }}</span></span>
						                                <span class="ec-productRole__priceRegularTax">{{ '(税込)'|trans }}</span>
						                            {% endif %}
						                        </div>
						                    {% else %}
						                        {% if Product.getPrice01Max is not null %}
						                            <span class="ec-productRole__priceRegularPrice">{{ '定価'|trans }}：{{ Product.getPrice01IncTaxMin|price }}</span>
						                            <span class="ec-productRole__priceRegularTax">{{ '(税込)'|trans }}</span>
						                        {% endif %}
						                    {% endif %}
						                    {# 販売価格 #}
						                    <div class="ec-productRole__price" style="font-size:20px; color:#000;border-top:1px dotted #ccc;">
						                        {% if Product.hasProductClass -%}
						                            {% if Product.getPrice02IncTaxMin == Product.getPrice02IncTaxMax %}
						                                <div class="ec-price">
						                                    <span class="ec-price__price price02-default">{{ Product.getPrice02IncTaxMin|price }}</span>
						                                    <span class="ec-price__tax">{{ '(税込)'|trans }}</span>
						                                </div>
						                            {% else %}
						                                <div class="ec-price">
						                                    <span class="ec-productRole__priceRegularPrice">{{ '価格'|trans }}：
						                                    </span><span class="ec-price__price price02-default" style="padding-left:0px;font-weight: normal;;">{{ Product.getPrice02IncTaxMin|price }}</span>
						                                    <span class="ec-price__tax">{{ '(税込)'|trans }}</span>
						                                </div>
						                            {% endif %}
						                        {% else %}
						                            <div class="ec-price">
						                                <span class="ec-price__price">{{ Product.getPrice02IncTaxMin|price }}</span>
						                                <span class="ec-price__tax">{{ '(税込)'|trans }}</span>
						                            </div>
						                        {% endif %}
						                    </div>
						                    
			                            </div>
			                            
			                            
			                            
			                            {# 定価(表示用) #}
					                    <!--
					                    {% if Product.hasProductClass -%}
					                        <div class="ec-productRole__priceRegular">
					                            {% if Product.getPrice01Min is not null and Product.getPrice01IncTaxMin == Product.getPrice01IncTaxMax %}
					                                <span class="ec-productRole__priceRegularPrice">{{ '定価'|trans }}：<span class="price01-view-default">{{ Product.getPrice01IncTaxMin|price }}</span></span>
					                                <span class="ec-productRole__priceRegularTax">{{ '(税込)'|trans }}</span>
					                            {% elseif Product.getPrice01Min is not null and Product.getPrice01Max is not null %}
					                                <span class="ec-productRole__priceRegularPrice">{{ '定価'|trans }}：<span class="price01-view-default">{{ Product.getPrice01IncTaxMin|price }}～ {{ Product.getPrice01IncTaxMax|price }}</span></span>
					                                <span class="ec-productRole__priceRegularTax">{{ '(税込)'|trans }}</span>
					                            {% endif %}
					                        </div>
					                    {% else %}
					                        {% if Product.getPrice01Max is not null %}
					                            <span class="ec-productRole__priceRegularPrice">{{ '定価'|trans }}：{{ Product.getPrice01IncTaxMin|price }}</span>
					                            <span class="ec-productRole__priceRegularTax">{{ '(税込)'|trans }}</span>
					                        {% endif %}
					                    {% endif %}]
					                    -->
					                    {# 販売価格(表示用) #}
					                    <!--
					                    <div class="ec-productRole__price" style="font-size:20px; color:#000;border-top:1px dotted #ccc;">
					                        {% if Product.hasProductClass -%}
					                            {% if Product.getPrice02IncTaxMin == Product.getPrice02IncTaxMax %}
					                                <div class="ec-price">
					                                    <span class="ec-price__price price02-view-default">{{ Product.getPrice02IncTaxMin|price }}</span>
					                                    <span class="ec-price__tax">{{ '(税込)'|trans }}</span>
					                                </div>
					                            {% elseif Product.getPrice02Min is not null and Product.getPrice02Max is not null %}
					                                <span class="ec-productRole__priceRegularPrice">{{ '定価'|trans }}：<span class="price02-view-default">{{ Product.getPrice02IncTaxMin|price }}～ {{ Product.getPrice02IncTaxMax|price }}</span></span>
					                                <span class="ec-productRole__priceRegularTax">{{ '(税込)'|trans }}</span>
					                            {% else %}
					                                <div class="ec-price">
					                                    <span class="ec-productRole__priceRegularPrice">{{ '価格'|trans }}：
					                                    </span><span class="ec-price__price price02-view-default" style="padding-left:0px;font-weight: normal;;">{{ Product.getPrice02IncTaxMin|price }}</span>
					                                    <span class="ec-price__tax">{{ '(税込)'|trans }}</span>
					                                </div>
					                            {% endif %}
					                        {% else %}
					                            <div class="ec-price">
					                                <span class="ec-price__price">{{ Product.getPrice02IncTaxMin|price }}</span>
					                                <span class="ec-price__tax">{{ '(税込)'|trans }}</span>
					                            </div>
					                        {% endif %}
					                    </div>
					                    -->
			                            
			                            
			                            <div class="ec-productRole__btn" style="width:100%;">
			                                <button type="submit" class="ec-blockBtn--action add-cart" style="outline:none; background: url({{ asset('assets/img/btn_cart.png') }}) no-repeat scroll 0 0; border-color:#fff;min-height:100px; height:auto;">
			                                </button>
			                            </div>
			                            
			                            <div class="ec-productRole__btn" style="width:100%;">
			                                <button type="button" class="ec-blockBtn--action back-main" style="outline:none; background: url({{ asset('assets/img/btn_back.png') }}) no-repeat scroll 0 0; border-color:#fff;min-height:100px; height:auto;">
			                                </button>
			                            </div>
			                            
				                            
		                            </div>
		                    <!-- .オプション一覧-->
		                 <!----- .【詳細を指定】ブロック ----->
		                 </div>
		                {% endif %}
		                <!-- .blk_syousai_sitei -->
		                 
                        {{ form_rest(form) }}
                        
                        
                         
                    <div class="ec-modal">
                        <div class="ec-modal-overlay">
                            <div class="ec-modal-wrap">
                                <span class="ec-modal-close"><span class="ec-icon"><img src="{{ asset('assets/icon/cross-dark.svg') }}" alt=""/></span></span>
                                <div id="ec-modal-header" class="text-center">{{ 'カートに追加しました。'|trans }}</div>
                                <div class="ec-modal-box">
                                    <div class="ec-role">
                                        <span class="ec-inlineBtn--cancel">{{ 'お買い物を続ける'|trans }}</span>
                                        <a href="{{ url('cart') }}" class="ec-inlineBtn--action">{{ 'カートへ進む'|trans }}</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!--
                    {% if BaseInfo.option_favorite_product %}
                        <form action="{{ url('product_add_favorite', {id:Product.id}) }}" method="post">
                            <div class="ec-productRole__btn">
                                {% if is_favorite == false %}
                                    <button type="submit" id="favorite" class="ec-blockBtn--cancel">
                                        {{ 'お気に入りに追加'|trans }}
                                    </button>
                                {% else %}
                                    <button type="submit" id="favorite" class="ec-blockBtn--cancel"
                                            disabled="disabled">{{ 'お気に入りに追加済です。'|trans }}
                                    </button>
                                {% endif %}
                            </div>
                        </form>
                    {% endif %}
                    -->
                    
                    </div>
                </div>
            </div>
        </div>
        
        
        
              
        </form>
        
    </div>
{% endblock %}